[project]
name = "cv-trafficsigns"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "vision-studio",
]

[tool.uv.sources]
vision-studio = { path = "../CV_VisionStudio", editable = true }
